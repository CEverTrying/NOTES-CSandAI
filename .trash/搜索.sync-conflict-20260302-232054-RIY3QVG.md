---
state:
 - 施工中
---

*这一大部分我已经学了不少了，实在懒得重写，~~等待之后施工吧~~*

一个搜索问题由以下要素组成：

- **状态空间** - 在给定世界中所有可能状态的集合  
- 每个状态下可用的**动作**集合  
- **转移**模型 - 在当前状态下执行特定动作时，输出下一个状态  
- 动作**代价**- 应用动作从一个状态转移到另一个状态时产生的代价  
- **初始状态**- 智能体最初所处的状态  
- **目标测试** - 以状态为输入，判断其是否为目标状态的函数

**状态空间（state space）**的大小：直接将不同状态的种类相乘

**状态空间图（state space graph）**：由不同的状态和箭头连接，状态不重复出现  
**搜索树**：状态可重复出现，由从上到下的树状结构表示  
![对比图](image/comparison.png)

---
## 无信息搜索(uninformed search)

这种搜索的特点是**只参考已经走过的状态，对还未走到的状态不进行参考/无法参考**  
搜索的伪代码是

```
function TREE-SEARCH(problem, frontier) return a solution or failure
    frontier ← INSERT(MAKE-NODE(INITIAL-STATE[problem]), frontier)
    while not IS-EMPTY(frontier) do
        node ← POP(frontier)
        if problem.IS-GOAL(node.STATE) then return node
        for each child-node in EXPAND(problem, node) do
            add child-node to frontier
    return failure
```

其中EXPAND函数的伪代码为

```
function EXPAND(problem, node) yields nodes
    s ← node.STATE
    for each action in problem.ACTIONS(s) do
        s' ← problem.RESULT(s, action)
        yield NODE(STATE=s', PARENT=node, ACTION=action)
```

| 名称         | DFS(深度优先) | BFS (广度优先) | UCS( 统一代价搜索)       |
| ------------ | ------------- | -------------- | ------------------------ |
| 必定实现目标 | 否            | 是             | 是                       |
| 最优         | 否            | 一般情况否     | 是                       |
| 时间复杂度(最坏)   | O(b^m)        | O(b^s)         | O(b^(C/ε))               |
| 空间复杂度(最坏)   | O(bm)         | O(b^s)         | O(b^(C/ε))               |
| 实现方式     | 后进先出      | 先进先出       | 基于堆(heap)的优先级排列[[数据结构#堆(heap)\|详情]] |


 注：
- m为最大深度，s为最浅目标状态的深度，C为最优路径的代价(cost),ε为最小的两个状态的代价